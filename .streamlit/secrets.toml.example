# Round Two — Streamlit secrets template.
#
# For local development: copy this file to .streamlit/secrets.toml
# (NOT committed — see .gitignore) and fill in your key.
#
# For Streamlit Community Cloud: paste these values into the
# Secrets UI for your app — do NOT commit the real secrets.toml.

ANTHROPIC_API_KEY = "sk-ant-replace-me"
